type Query {
  newsList: [News]
  news(id: String!): News
}

type Mutation {
  createNews(createNewsInput: CreateNewsInput): News
}

type Subscription {
  newsCreated: News
}

type News {
  _id: String!
  title: String
}

input CreateNewsInput {
  name: String
  age: Int
}
