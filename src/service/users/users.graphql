type Query {
  usersList: [User]
  user(id: String!): User
}

type Mutation {
  createUser(createUserInput: CreateUserInput): User
}

type User {
  _id: String
  nickname: String
}

input CreateUserInput {
  name: String
  age: Int
}
