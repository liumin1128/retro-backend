type Query {
  oauthsList: [OAuth]
  oauth(id: ID!): OAuth
}

type Mutation {
  createOAuth(createOAuthInput: CreateOAuthInput): OAuth
}

type OAuth {
  _id: ID
  nickname: String
}

input CreateOAuthInput {
  name: String
  age: Int
}
